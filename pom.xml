<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <artifactId>document-generator</artifactId>
  <version>unversioned</version>
  <packaging>pom</packaging>
  <name>document-generator</name>
  <description>Main Repo holding the sub modules for the generation of documents</description>

  <url>http://maven.apache.org</url>
  <parent>
    <groupId>uk.gov.companieshouse</groupId>
    <artifactId>companies-house-parent</artifactId>
    <version>2.1.14</version>
    <relativePath/>
  </parent>

  <properties>
    <!-- Java and Spring -->
    <jdk.version>21</jdk.version>
    <spring-boot-dependencies.version>4.1.0</spring-boot-dependencies.version>

    <!-- CH dependencies -->
    <ch-boms.version>0.1.16</ch-boms.version>
    <api-sdk-java.version>7.2.3</api-sdk-java.version>
    <private-api-sdk-java.version>7.2.19</private-api-sdk-java.version>
    <company-accounts-library.version>3.0.1</company-accounts-library.version>

    <!-- external libraries not managed by the CH BOMs or Spring dependencies -->
    <json.version>20240303</json.version>
    <commons-io.version>2.20.0</commons-io.version>
    <httpclient5.version>5.4.4</httpclient5.version>

    <!--
      mapstruct itself is managed by ch-core-bom. This is only for the
      annotation processor path, which no BOM manages, so keep it in step
      with the mapstruct version the BOM resolves to.
    -->
    <org.mapstruct.version>1.6.3</org.mapstruct.version>

    <!-- Maven dependencies -->
    <spring-boot-maven-plugin.version>4.1.0</spring-boot-maven-plugin.version>
    <maven-compiler-plugin.version>3.14.0</maven-compiler-plugin.version>
    <maven-surefire-plugin.version>3.5.2</maven-surefire-plugin.version>
    <maven-jar-plugin.version>3.4.2</maven-jar-plugin.version>
    <maven-install-plugin.version>3.1.4</maven-install-plugin.version>
    <jacoco-maven-plugin.version>0.8.12</jacoco-maven-plugin.version>

    <!-- Repositories -->
    <artifactoryResolveSnapshotRepo>libs-snapshot-local</artifactoryResolveSnapshotRepo>
    <artifactoryResolveReleaseRepo>libs-release-local</artifactoryResolveReleaseRepo>

    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>uk.gov.companieshouse</groupId>
        <artifactId>ch-dependency-bom</artifactId>
        <version>${ch-boms.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>uk.gov.companieshouse</groupId>
        <artifactId>ch-test-bom</artifactId>
        <version>${ch-boms.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot-dependencies.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!--
       external libraries managed by neither the CH BOMs nor Spring dependencies
       -->
      <dependency>
        <groupId>org.json</groupId>
        <artifactId>json</artifactId>
        <version>${json.version}</version>
      </dependency>

      <dependency>
        <groupId>org.jacoco</groupId>
        <artifactId>org.jacoco.report</artifactId>
        <version>${jacoco-maven-plugin.version}</version>
      </dependency>

      <!--
       Deliberate overrides of a BOM-managed version.
       Remove once the BOM catches up, so that we track the BOM again.
      -->

      <!-- ch-core-bom pins 2.17.0; keep the newer release here -->
      <dependency>
        <groupId>commons-io</groupId>
        <artifactId>commons-io</artifactId>
        <version>${commons-io.version}</version>
      </dependency>

      <!-- CVE-2025-27820 -->
      <dependency>
        <groupId>org.apache.httpcomponents.client5</groupId>
        <artifactId>httpclient5</artifactId>
        <version>${httpclient5.version}</version>
      </dependency>

      <!--
       CH Libs
       -->
      <dependency>
        <groupId>uk.gov.companieshouse</groupId>
        <artifactId>api-sdk-java</artifactId>
        <version>${api-sdk-java.version}</version>
      </dependency>

      <dependency>
        <groupId>uk.gov.companieshouse</groupId>
        <artifactId>private-api-sdk-java</artifactId>
        <version>${private-api-sdk-java.version}</version>
      </dependency>

      <dependency>
        <groupId>uk.gov.companieshouse</groupId>
        <artifactId>company-accounts-library</artifactId>
        <version>${company-accounts-library.version}</version>
      </dependency>

    </dependencies>
  </dependencyManagement>

  <!-- do NOT add version numbers in dependencies - these belong in dependencyManagement -->
  <dependencies>
    <!-- Spring managed Libraries -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-hateoas</artifactId>
      <scope>compile</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-logging</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
    </dependency>

    <!-- CH Libraries -->
    <dependency>
      <groupId>uk.gov.companieshouse</groupId>
      <artifactId>api-sdk-java</artifactId>
    </dependency>
    <dependency>
      <groupId>uk.gov.companieshouse</groupId>
      <artifactId>structured-logging</artifactId>
    </dependency>

    <!-- external libraries not managed by Spring dependencies -->
    <dependency>
      <groupId>org.sonarsource.scanner.maven</groupId>
      <artifactId>sonar-maven-plugin</artifactId>
      <version>${sonar-maven-plugin.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mapstruct</groupId>
      <artifactId>mapstruct</artifactId>
    </dependency>
    <dependency>
      <groupId>tools.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>
    <dependency>
      <groupId>com.google.code.gson</groupId>
      <artifactId>gson</artifactId>
    </dependency>
    <dependency>
      <groupId>org.json</groupId>
      <artifactId>json</artifactId>
    </dependency>

    <!-- Testing -->
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <profiles>
    <!-- dev is the profile that is activated by default. This is only for developers as it does
         not publish to any repository. It just downloads dependencies, compiles code and builds jar
         for running the application and then runs the tests. -->
    <profile>
      <id>dev</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <build>
        <finalName>${project.artifactId}-${project.version}</finalName>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-install-plugin</artifactId>
              <version>${maven-install-plugin.version}</version>
            </plugin>
            <plugin>
              <groupId>org.springframework.boot</groupId>
              <artifactId>spring-boot-maven-plugin</artifactId>
              <configuration>
                <mainClass>uk.gov.companieshouse.document.generator.api.DocumentGeneratorApplication
                </mainClass>
                <layout>ZIP</layout>
              </configuration>
              <executions>
                <execution>
                  <goals>
                    <goal>repackage</goal>
                  </goals>
                </execution>
              </executions>
            </plugin>
            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-compiler-plugin</artifactId>
              <version>${maven-compiler-plugin.version}</version>
              <configuration>
                <source>${jdk.version}</source>
                <target>${jdk.version}</target>
                <fork>true</fork>
                <meminitial>128m</meminitial>
                <encoding>${project.build.sourceEncoding}</encoding>
                <maxmem>512m</maxmem>
                <annotationProcessorPaths>
                  <path>
                    <groupId>org.mapstruct</groupId>
                    <artifactId>mapstruct-processor</artifactId>
                    <version>${org.mapstruct.version}</version>
                  </path>
                </annotationProcessorPaths>
              </configuration>
            </plugin>
            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-jar-plugin</artifactId>
              <version>${maven-jar-plugin.version}</version>
            </plugin>

            <plugin>
              <groupId>org.jacoco</groupId>
              <artifactId>jacoco-maven-plugin</artifactId>
              <version>${jacoco-maven-plugin.version}</version>
            </plugin>
            <plugin>
              <groupId>com.google.cloud.tools</groupId>
              <artifactId>jib-maven-plugin</artifactId>
              <version>${maven-jar-plugin.version}</version>
              <configuration>
                <from>
                  <image>416670754337.dkr.ecr.eu-west-2.amazonaws.com/ci-corretto-build-21:latest
                  </image>
                </from>
                <to>
                  <image>416670754337.dkr.ecr.eu-west-2.amazonaws.com/document-generator:latest
                  </image>
                </to>
                <container>
                  <expandClasspathDependencies>true</expandClasspathDependencies>
                </container>
                <extraDirectories>
                  <paths>
                    <path>
                      <from>../document-generator-common/api-enumerations</from>
                      <into>/document-generator-common/api-enumerations</into>
                    </path>
                  </paths>
                </extraDirectories>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>
      </build>
    </profile>
  </profiles>

  <modules>
    <module>document-generator-interfaces</module>
    <module>document-generator-accounts</module>
    <module>document-generator-prosecution</module>
    <module>document-generator-api</module>
    <module>document-generator-company-report</module>
    <module>document-generator-common</module>
  </modules>
</project>
